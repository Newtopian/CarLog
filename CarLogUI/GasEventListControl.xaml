<UserControl x:Class="CarLog.GasEventListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarLog"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             VerticalAlignment="Stretch"
             HorizontalAlignment="Stretch">
    <Grid>
        <ListView x:Name="GasEventListView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
            <!-- <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="ActualHeight">
                
            </Binding>
            -->
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
